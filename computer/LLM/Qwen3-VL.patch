diff --git a/qwen-vl-finetune/qwenvl/data/__init__.py b/qwen-vl-finetune/qwenvl/data/__init__.py
index 68d3dea..7209385 100755
--- a/qwen-vl-finetune/qwenvl/data/__init__.py
+++ b/qwen-vl-finetune/qwenvl/data/__init__.py
@@ -26,12 +26,19 @@ VIDEOCHATGPT = {
     "data_path": "PATH_TO_VIDEOCHATGPT_DATA",
 }
 
+
+VLA_SFT = {
+    "annotation_path": "/workspace/Qwen3-VL/qwen-vl-finetune/demo/image_per_turn.json",
+    "data_path": "/workspace/Qwen3-VL/qwen-vl-finetune/demo/",
+}
+
 data_dict = {
     "cambrian_737k": CAMBRIAN_737K,
     "cambrian_737k_pack": CAMBRIAN_737K_PACK,
     "mp_doc": MP_DOC,
     "clevr_mc": CLEVR_MC,
     "videochatgpt": VIDEOCHATGPT,
+    "vla_sft": VLA_SFT,
 }
 
 
diff --git a/qwen-vl-finetune/qwenvl/data/data_processor.py b/qwen-vl-finetune/qwenvl/data/data_processor.py
index 8c17bf6..41dee14 100644
--- a/qwen-vl-finetune/qwenvl/data/data_processor.py
+++ b/qwen-vl-finetune/qwenvl/data/data_processor.py
@@ -1,3 +1,4 @@
+import os
 import json
 import random
 import logging
@@ -26,6 +27,7 @@ DEFAULT_VIDEO_TOKEN = "<video>"
 
 local_rank = None
 
+PRINT_SFT_PROMPTS = os.environ.get("PRINT_SFT_PROMPTS", "0") == "1"
 
 def rank0_print(*args):
     if local_rank == 0:
@@ -438,6 +440,12 @@ class LazySupervisedDataset(Dataset):
         ]
         label = self.processor.tokenizer.decode(labels, skip_special_tokens=False)
 
+        if PRINT_SFT_PROMPTS:
+            print("#####################")
+            print("# SFT sample prompt #")
+            print("#####################")
+            print(text)
+
         return data_dict
 
     def _get_packed_item(self, sources) -> Dict[str, torch.Tensor]:
@@ -691,4 +699,4 @@ def make_supervised_data_module(processor, data_args) -> Dict:
 
 
 if __name__ == "__main__":
-    pass
+    pass
\ No newline at end of file
diff --git a/qwen-vl-finetune/qwenvl/train/train_qwen.py b/qwen-vl-finetune/qwenvl/train/train_qwen.py
index f67d569..d5cbc0c 100644
--- a/qwen-vl-finetune/qwenvl/train/train_qwen.py
+++ b/qwen-vl-finetune/qwenvl/train/train_qwen.py
@@ -138,6 +138,17 @@ def train(attn_implementation="flash_attention_2"):
         model_args.model_name_or_path,
     )
 
+    try:
+        print("train: loading custom chat template from 'chat_template.jinja'")
+        with open("/workspace/Qwen3-VL/qwen-vl-finetune/chat_template.jinja", "r") as f:
+            custom_chat_template = f.read()
+        processor.chat_template = custom_chat_template
+        processor.tokenizer.chat_template = custom_chat_template
+        print("train: successfully replaced default chat template with custom template")
+    except Exception as e:
+        print(f"train: error: failed to load custom chat template: {str(e)}")
+        sys.exit(1)
+
     if data_args.data_flatten or data_args.data_packing:
         replace_qwen2_vl_attention_class()
     model.config.use_cache = False
